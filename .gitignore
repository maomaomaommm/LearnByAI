node_modules
.next
.learnbyai/
.env
.env*.local
.tools
npm-debug.log*
.dev-server*.log
.tunnel*.log
playwright-report/
test-results/

Kimi_Agent_LearnByAI/
Kimi_Agent_LearnByAI.zip

# Python cache & local logs & scratch
__pycache__/
*.pyc
dev.log
tmp/

# Deploy script embeds plaintext SSH credentials — keep out of VCS until secrets
# are externalized to env vars / key auth (see TODO P1-4). Do NOT commit as-is.
deploy_remote.py

# TS incremental build cache (build artifact)
tsconfig.tsbuildinfo
